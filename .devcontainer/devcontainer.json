{
  "image": "mcr.microsoft.com/devcontainers/javascript-node:20-bullseye",
  "tasks": {
    "dev": "npm install && npm run dev",
    "build": "npm install && npm run build",
    "start": "npm install && npm run start",
    "lint": "npm install && npm run lint"
  }
}
